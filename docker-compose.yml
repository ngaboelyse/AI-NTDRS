services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ai_ntdrs}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ai_ntdrs}
      POSTGRES_DB: ${POSTGRES_DB:-ai_ntdrs}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - db

volumes:
  postgres_data:
